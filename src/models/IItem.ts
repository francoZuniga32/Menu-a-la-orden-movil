export default interface Item {
        id: number | null,
        titulo: string,
        precio: number,
        descripcion: string,
        foto: string | null,
        idMenu: number | null
    }